# buildUponDefaultConfig is active; add only the rules that require explicit configuration here
potential-bugs:
  Deprecation:
    active: true

formatting:
  MaximumLineLength:
    active: false
    maxLineLength: 180

style:
  MaxLineLength:
    active: true
    maxLineLength: 180
  WildcardImport:
    # covered by ktlint and diktat
    active: false
